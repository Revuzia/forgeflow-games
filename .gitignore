node_modules/
dist/
.env
*.local
.DS_Store

# 2026-05-11 — Downloaded asset cache (re-downloadable per pipeline/assets/_downloaded/TASKS.md)
# Previously committed; 8.3 GB working tree + 1.15 GB mocap zip in pack history
# caused GitHub HTTP 500 on push. History purged via git filter-repo.
pipeline/assets/_downloaded/

# Wrangler local-dev state (per-machine, not portable)
.wrangler/state/

.secrets/

# XCOM autopipe runtime artifacts
pipeline/engine/_match_*.png
pipeline/engine/xcom_match_report.json
pipeline/engine/xcom_autopipe_log.jsonl

# 2026-06-02 — Bulk CC0/CC-BY creature library (~820 MB, 1,500+ models from Quaternius /
# Poly Pizza / Kenney / OpenGameArt). Kept OUT of git: the 2026-05-11 large-asset push
# already hit GitHub HTTP 500 + needed a history purge — don't repeat it. Binaries are
# local + re-downloadable (per-model source_url in CREATURES_INDEX.json, which IS tracked).
pipeline/assets/3d-models/polypizza/
pipeline/assets/3d-models/quaternius/
pipeline/assets/3d-models/kenney-creatures/
pipeline/assets/3d-models/opengameart-creatures/

# 2026-06-08 — ForgeFlow-Engine game builds (Phase-A wiring, pipeline/engine/engine_game_build.py).
# Each bundles a copy of the engine runtime + scene.json; reproducible from content.json + the engine
# repo, and deployed to R2. Keep OUT of git (don't duplicate the engine / bloat history).
games/_engine/

# 2026-06-10 — Unity Asset Store haul (owner-licensed, NOT redistributable; regenerated nightly
# from F:\games cache by scripts/unity_ingest_and_wire.py — committing would repeat the 8.3GB
# push incident above AND violate the Asset Store license)
pipeline/assets/3d-models/unity/
pipeline/assets/3d-models/UNITY_MODELS_INDEX.json
pipeline/assets/UNITY_ASSETS_INDEX.json

# 2026-06-10 audit Q3 — runtime/test artifacts in pipeline/engine never belong in git
pipeline/engine/__pycache__/
pipeline/engine/gates/__pycache__/
pipeline/engine/_wip/
pipeline/engine/_captest.py
pipeline/engine/_capfix_test.png
pipeline/engine/*.png.lit

asset_gen/
.claude/worktrees/
.wrangler/
