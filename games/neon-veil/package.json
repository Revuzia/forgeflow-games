{
  "name": "neon-veil",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Neon Veil — local rebuild of the restored TypeScript sources (no deploy). Edit src-restored/src/*.ts, then `npm run build`; output lands in dist-rebuild/ (kept separate from the live bundle in assets/).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.170.0"
  },
  "devDependencies": {
    "@types/three": "0.170.0",
    "typescript": "~5.5.4",
    "vite": "^6.0.0"
  }
}
