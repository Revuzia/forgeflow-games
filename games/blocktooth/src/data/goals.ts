// BLOCKTOOTH v2 — the 40 goals (FEATURES_V2 §8.2). THREE-free data.
//
// ── L0 SKELETON STUB ── empty: lane L5 writes the catalogue.

import type { GoalDef } from '../core/types.ts';

export const GOALS: GoalDef[] = [];
