{
  "name": "blocktooth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BLOCKTOOTH — isometric kaiju survivor-like. Three.js + TypeScript + Vite. `npm install && npm run dev`.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "probe": "node _harness/probe_sim.ts",
    "check": "npm run typecheck && npm run probe"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "0.20.0",
    "@fontsource/anton": "^5.3.0",
    "@fontsource/barlow-condensed": "^5.3.0",
    "@fontsource/space-mono": "^5.3.0",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/three": "0.186.0",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
