# Last Circle — Build Order (105 verified findings, merged to 88 tasks)

Ranked by player-visible impact ÷ (risk × effort). Sizes: **S** ≤ 15 min / one-liner, **M** ≤ 2 h / one file, **L** multi-file or needs playtest.

---

## 1. DO NOW — high impact, low risk

| # | Title | file:line | Fix | Size |
|---|---|---|---|---|
| 1 | Sniper ADS blacks out the entire HUD behind the 92%-black scope surround | `runtime/3d/royale/hud.js:1252` | Move `R.scope` + its two `scLine` children to immediately after `const L = layer("hud")` (:1147) so it is the bottom-most child (verified: scope is currently created after `hurtTint`, near-last). | S |
| 2 | The click that re-acquires pointer lock also fires your weapon (every alt-tab return costs a round) | `runtime/3d/royale/player.js:493-499` | `const wasLocked = document.pointerLockElement === dom; tryLock(); if (e.button === 0 && !wasLocked) return;` before the button-0 block. Leave button 2 alone. | S |
| 3 | SPACE key-repeat gate is dead code — holding SPACE strobes the parachute | `runtime/3d/royale/player.js:443` | Add `repeat: ev.repeat` to the synthetic `e` object (verified: `:449` reads `!e.repeat` off an object that has no `repeat`). | S |
| 4 | Head hitbox is the full 1.14 m body cylinder — 2.5× sniper headshots on visible misses | `runtime/3d/royale/weapons.js:510` | `const isHead = py > feetY + (headY-feetY)*0.86 && dh < 0.20;` — leave the registration test at `:507` untouched. Export HEAD_BAND/HEAD_R from `sim/royale.js`. | S |
| 5 | Remote actors never animate — every other player (and all 45 bots on a guest) slides frozen in the jump clip | `runtime/3d/royale/player.js:1012` (interpRemote) | Derive from the lerp: cache `px/pz`, then set `a.vel` from the delta (clamp 12 m/s) and `a.onGround = !a.gliding && a.pos.y <= supportAt(...) + 0.2`. No protocol change. **Merges the `player.js:959` and `net.js:329` findings.** | M |
| 6 | Bots never cut their canopy and cannot shoot while gliding — free 12-17 s farming window | `runtime/3d/royale/bots.js:349` | Track `bb.chuteCut`; set `inp.jump = true` once when `a.chute && (a.pos.y - heightAt) > 60 && d < 120`. Ship part (a) only; the hurtActor glide-immunity half is a balance call. | M |
| 7 | Mouse-sensitivity sliders bottom out at literally 0 and persist — bricks the install | `runtime/3d/royale/hud.js:2203` + `ffg_royale3d.js:392` | Clamp in `loadSettings` (self-heals poisoned storage): `s.sensitivity = Math.min(3, Math.max(0.15, +s.sensitivity || 1))`, same for `adsSensitivity`/`fov`; give `slider()` a min arg. | S |
| 8 | Quick Match ground-spawns ~48% of the lobby outside the starting circle; corner spawns can't reach circle 1 | `runtime/3d/royale/player.js` spawnAll :364-367 | Rejection-loop `randomGroundPos` against `W.stormCtl.storm.circles[0]` (≤30 tries, then push radially to 0.85 r). Guard `if (c0)` for practice mode. | M |
| 9 | Long-range kills produce a mid-stride statue — anim LOD freezes the mixer and death never unfreezes it | `runtime/3d/royale/player.js:1137` | One line in `killActor`: `if (victim.rig && victim.rig.mixer) victim.rig.mixer.timeScale = 1;`. (Raising the 250 m threshold is the separate perf item.) | S |
| 10 | Remote emotes are overwritten one frame after they start | `runtime/3d/royale/player.js:952` | `if (!far && !a.emoting) {` — one token. | S |
| 11 | Fullscreen map (M) neither suppresses fire/ADS nor releases input | `runtime/3d/royale/hud.js:1944-1952` | Set `W.uiModal` true/false on open/close; add `|| W.uiModal` to `player.js:463`, `:528` and the unguarded ADS latch at `:566`. Delete the dead `L.onclick`. Do **not** add a pause (breaks online). | S |
| 12 | Host-simulated bot hits on a guest are credited to the HOST player (wrong killer name, kill count, spectate target) | `runtime/3d/royale/net.js:32` | Add a 5th `attackerId` param to `reportRemoteHit`/`reportHit`; pass `p.ownerId` at `weapons.js:514` and `ownerId` at `:609`. Additive, `||` fallback preserves behaviour. | S |
| 13 | A friend who backs out of the lobby before START still gets a storm-immune roster slot that can win | `runtime/3d/royale/net.js:123` | Part (1) only: prune `S.peers` against the presence `ids` list on every `peer` event. (Part 2, the watchdog seed, goes to DO NEXT.) | S |
| 14 | Every gunshot of a class is a bit-identical waveform | `runtime/3d/royale/audio.js:120-140` | Four in-place edits in `shot()`: playbackRate 0.93-1.07, `n.start(t, Math.random()*0.7)`, ±8% cutoff jitter, ±10% gain jitter. | S |
| 15 | No limiter on the master bus + phase-coherent 9-pellet impact sum clips | `runtime/3d/royale/audio.js:29-35`, `:262` | Insert a `DynamicsCompressor` (thr −6, ratio 20, knee 0) between master and destination; add a 35 ms `lastImpactT` throttle on the impact handler. | S |
| 16 | A 250 m shot sounds identical to a point-blank one | `runtime/3d/royale/audio.js:85-118` | Tag `g.__d/__maxD` in `spatial()`; scale both filter cutoffs by `0.25 + 0.75*df` and gate the crack layer on `__d < 80`. `__d != null` keeps the player's own shots bright. | M |
| 17 | Ground-item pickup permanently blocks the HOLD-E chest channel | `runtime/3d/royale/loot.js:580` | Prefer the chest while E is **held**, nearest-item on tap: `const target = (a.input.interactDown && chestNear) ? chestNear : (itemNear || chestNear) \|\| null;` | S |
| 18 | Page Pause button is wired to `window.__PAUSE__`, which Last Circle never defines | `game_controls.js:199` / `ffg_royale3d.js:84` | Export phase-gated `requestPause(W, forceOpen)` from hud.js; assign `window.__PAUSE__` right after `hudMod.init(W)`. Routes through `togglePause` so the online no-freeze branch survives. | S |
| 19 | Shell's global F = fullscreen fires on any action rebound to F, including mid-rebind-capture | `index.html:11` | `window.GAME_CONFIG={hide_bug_button:true,mute_hotkey:false,fs_hotkey:false}` — mirrors the existing `mute_hotkey` precedent. | S |
| 20 | ESC is dead on the main menu — Settings and How To Play can only be closed with their button | `runtime/3d/royale/player.js:445` | Hoist the Escape check above the `if (!W.player \|\| W.phase === "menu") return;` guard at `:445`. Check hud's `escPressed` handler doesn't assume `W.player`. | S |
| 21 | Right Shift is an invisible second sprint key that survives rebinding and RESET | `runtime/3d/royale/player.js:439/455/553` | Fold `ShiftRight → ShiftLeft` in `canon()`, drop the two raw `ShiftRight` reads. **Constraint guard: SHIFT stays a toggle — do not touch `sprintToggle`.** | S |
| 22 | The whole 240-270 m glider descent renders with zero shadows | `runtime/3d/ffg_royale3d.js:173` | Anchor `_sunFocus.y` to `Math.min(f.pos.y, W.map.groundAt(x,z) + 8)`; also `sc.far = 600` at `:121`. | S |
| 23 | GLB textures never receive the tier's anisotropy | `runtime/3d/ffg_royale3d.js:141`, `:271` | Call `W.applyGraphics(W.settings.graphics)` at the end of `_startMatch` after `loadActorModels`; add `"emissiveMap"` to the slot list. Do **not** edit the vendored kernel. | S |
| 24 | "Low" graphics tier still runs the full UnrealBloomPass | `runtime/3d/ffg_royale3d.js:103` | `const BLOOM={low:0,medium:0.14,high:0.14};` + `kernel.bloom.enabled = BLOOM[tier] > 0;`. | S |
| 25 | Rubble Row shatters into 16 one-cube draw calls (batcher keys on a random float) | `runtime/3d/royale/maps.js:864` | `shade(C_BRICKB, 0.8 + Math.floor(rng()*4)*0.1)` — 16 batches → 4. Make `shade()` snap to 0.05 steps so no future site can reintroduce it. | S |
| 26 | 151 sprites with 151 distinct SpriteMaterials; each portal builds 3 fresh geos+mats | `runtime/3d/royale/maps.js:357/373` | Hoist one `cloudMat`, one `birdMat`, and one portal geo/mat triple per `buildMap`. `disposeMapResources` already dedupes by identity. | S |
| 27 | Shadow pass redraws every car/container/barrel/rock: +1.41 M tris, +21 draw calls/frame | `runtime/3d/royale/maps.js:1111` | Interim one-liner: `inst.castShadow = kind !== "bush" && !["car","container","barrel","rocks"].includes(kind);` | S |
| 28 | Particle pool draws 1024 instances and re-uploads 64 KB every frame, even at zero live particles | `runtime/3d/royale/fx.js:25/46/56/217` | (a) capture slot index at spawn instead of `indexOf`; (b) hoist `_Z` axis vector; (c) `inst.count = nLive === 0 ? 0 : N` and only flag `needsUpdate` on frames that wrote. **Do not use the audit's high-water-mark — the ring buffer pins it to N.** | S |
| 29 | Damage numbers stack ~12 deep into one illegible column | `runtime/3d/royale/fx.js:174/191/236` | Jitter spawn ±0.35 xz / +0-0.35 y, and store a per-number `riseV: 0.7 + rand*0.5`. | S |
| 30 | Impact colour table has a dead `build` branch and paints wood as dirt | `runtime/3d/royale/fx.js:112` | Module-scope `IMPACT_COLOR = {flesh:0xc23b3b, wood:0xd7c08a, stone:0xa9a9a9, dirt:0x9a7f4f}` + `[surface] \|\| dirt`. Deletes the building-era branch. | S |
| 31 | Water is metalness>0 with no env map: −30% albedo **and** a clipped swimming white glint | `runtime/3d/royale/maps.js:315` | `roughness: 0.30, metalness: 0.0`. **Merges the env-map-water and NoToneMapping-glint findings.** Do not revert NoToneMapping. | S |
| 32 | Ranger Ridge / Fire Watch decks are ringed on all four sides and the ramp arrives head-on into a rail | `runtime/3d/royale/maps.js:556` | Split the +Z rail into two 1.6 m stubs and extend `addRamp` to `z + P + 2.2` so it reaches the deck edge. | S |
| 33 | No critical-health state — the HP bar is the same green at 6 HP as at 100 | `runtime/3d/royale/hud.js:1329` | Inside the existing change-gated branch: red ≤25, amber ≤40, plus `lcPulse` (already defined) under 25. Zero per-frame cost. | S |
| 34 | "I'm being shot" vignette is driven from a 4 Hz block | `runtime/3d/royale/hud.js:1422/1827` | Set opacity 1 instantly in the `actorHurt` handler **before** the attackerId guard (so storm ticks flash too); make `:1422` clear-only after 0.7 s. | S |
| 35 | Reload bar is half-hidden behind the interact hint below ~1160 px height | `runtime/3d/royale/hud.js:1177` | `top: "55%"` (verified clear of healBar 58% and sprintPip 66.5%). Not a z-index fix. | S |
| 36 | Perf chip paints over the second row of the kill feed | `runtime/3d/royale/hud.js:1261` | `top: "44px"`. | S |
| 37 | Locker preview renders the character in bind pose until two extra GLB fetches resolve | `runtime/3d/royale/hud.js:718` | `const base = rig.animations[0]; if (base) rig.play(base.name);` — the cheer/dance crossfades off it. | S |
| 38 | "Survived" stat, survival XP and lifetime timeAlive all measure MATCH length, not your life | `ffg_royale3d.js` buildPostMatch | `timeS: Math.round((W.match.feed.find(f => f.victim === W.player.id) \|\| {}).t ?? W.t)` — fixes display, XP and career counter together. | S |
| 39 | Post-match "XP earned" silently omits every point of challenge XP | `runtime/3d/royale/hud.js:1449/2083` | Accumulate `W._chalXp` at the award site, reset in `showHUD`, render as its own row + TOTAL. Do **not** re-award. | S |
| 40 | Mode cards are non-focusable `<div>`s — a keyboard user can never start a match | `runtime/3d/royale/hud.js:537` | `c.tabIndex=0; role="button"; onkeydown` Enter/Space → `c.onclick()`. **`mapId: randomMap()` stays — no picker.** | S |
| 41 | Every settings toggle/rebind rebuilds the panel and throws scroll to the top | `runtime/3d/royale/hud.js:2289` | Capture `firstChild.scrollTop` at the top of `showSettings`, restore after `closeB` is appended. | S |
| 42 | No invert-Y option in a game that ships a full remap screen | `runtime/3d/royale/player.js:532` | `invertY:false` default + one OFF/ON row + `* (W.settings.invertY ? -1 : 1)`. | S |
| 43 | Camera shake has no intensity control and `prefers-reduced-motion` is never consulted | `runtime/3d/royale/player.js:1071` | `shake:1` default (seed 0 when the media query matches), one slider, multiply at the single read site. **Prerequisite for DO-NEXT #58.** | S |
| 44 | `playerName` is in the schema and read in two places but no screen sets it | `runtime/3d/ffg_royale3d.js:254` / `hud.js:2204` | 16-char text input at the top of `showSettings` + mirror on the SQUAD UP panel. `player.js:441` already ignores INPUT targets. | S |
| 45 | Interact prompt always reads "Pick up" even when E throws your active slot on the ground | `runtime/3d/royale/loot.js:594` → `hud.js:1481` | Stamp `swap`/`outgoing` onto `W.interactHint`; branch the label to "[E] Swap for X — drops Y". `shortName` already exists at `hud.js:1713`. | M |
| 46 | Module header + sniper reticle advertise bullet drop that does not exist | `weapons.js:5-9/416`, `hud.js:1660` | Declare flat: delete the drop tick from the reticle, `gravity: def.arc ? -18 : 0`, correct the header. **Do not wire real gravity — `bots.js:581` only compensates for `arc` weapons.** | S |

---

## 2. DO NEXT — high impact, needs care

| # | Title | file:line | Fix | Size |
|---|---|---|---|---|
| 47 | **Projectiles resolve against actors BEFORE world geometry — you can shoot anyone within ~2.4 m of the far side of a wall** | `runtime/3d/royale/weapons.js:494-534` | Hoist `K.segmentColliders` above the actor loop, use its `t` as a range clamp, track `bestU` and apply damage after the loop; reuse the computed `sh` for the static branch. Highest-impact correctness bug in the file. | L |
| 48 | Palm Bay's 8 houses are geometrically scrambled — `rotY` spins each wall in place, producing a pinwheel of loose slabs | `runtime/3d/royale/maps.js:434`, house() 626-638 | Add `addBoxR(x,z,dx,dz,...)` that rotates the offset as well as the geometry; rewrite `house()` in local space. `hut()` has the same latent bug (all call sites currently rot=0). | M |
| 49 | `gunScore` ranks the sniper below the starter pistol — bots effectively never wield the strongest gun | `runtime/sim/royale.js:75` + `bots.js` slotScore | Add explicit `value` per weapon (pistol 40 … sniper 120); make `bots.js slotScore` delegate to `K.gunScore` keeping its dry-gun guard. **Both files in one commit** — royale.js alone is worse than status quo. Verify by watching a bot fire a sniper. | M |
| 50 | `inp.sprint` is dead inside ENGAGE — bots close at 3.8 m/s, slower than a walking player | `runtime/3d/royale/bots.js:560/606` | `inp.sprint = dist > prefer*1.8;` and `inp.ads = dist > 25 && !inp.sprint && speed < 5`. Re-check tier `aimErrDeg` after — hipfire cone doubles while closing. Does **not** touch player sprint toggle. | M |
| 51 | A bot at 59 HP pops an 8 s medkit and becomes a rooted, non-firing, non-abortable target | `runtime/3d/royale/bots.js:147/314/501` | Ship part (3) first (bail out of HEAL when a target is seen within 0.5 s) — low risk on its own. Then heal-tiering, then the `cancelHeal` refund path (must re-create the slot; `player.js:1164` decrements before setting `a.healing`). | M |
| 52 | Re-peeking a bot is free — reaction delay and acquire overshoot both key off `bb.acquireT`, stamped only on ID change | `runtime/3d/royale/bots.js:218` | Re-stamp on re-acquisition at half reaction cost; **ship the acquireT half first**, the `l.212` cone-bypass recency gate second. Global difficulty change across 49 bots. | M |
| 53 | The 12 s silent-guest watchdog ejects you without telling you — you keep playing a ghost that still deals damage | `runtime/3d/royale/net.js:233` | Handle the self case (panel + `leave(W)`), and drive the 12 Hz state send from a `setInterval(…,83)` started in `beginOnlineMatch`/cleared in `leave()` so a backgrounded tab keeps the link. Interval must be idempotent with rAF. | M |
| 54 | Transport is configured for 12 events/sec while the host emits 22-100+/sec; gunfire relay has no distance cull | `runtime/net/ffg_netplay.js:43`, `net.js:47/115` | Parameterise `eventsPerSecond` (pass 60 from net.js), cull fire relay against **every human position**, not the host's, then coalesce fire into the 10 Hz `bots` message. Land the constant change in `pipeline/engine` too. | M |
| 55 | Chunk the map-wide InstancedMeshes — 2.85 M of 3.01 M frame triangles are props that never cull (and the same pass feeds the shadow map) | `runtime/3d/royale/maps.js:1110` + batches at `:1065` | Bucket `props[kind]` and structure `batches` into 200 m cells (64 cells / 1600 m). **`destroyProp` at :1243 resolves `instRefs[col.prop]` by flat `col.idx` — store `{cell, localIdx}` or shot props vanish at the wrong index.** Merges both perf findings. | L |
| 56 | Guest watchdog can reap a slot that never reported (slow-loading guest gets bot-swapped before spawning) | `runtime/3d/royale/net.js:305`, `beginOnlineMatch` | Seed `S.lastSeen` in `onMatchStart` (net.js:276), not at roster build, or raise the threshold to ~20 s. Part 2 of #13. | S |
| 57 | Every tracer evaporates at 44 m — sniper and AR shots have no visible streak at their tuned ranges | `runtime/3d/royale/fx.js:101` | Adopt the `K = W.SIM` module pattern, derive life from `falloff[1]/speed` clamped 0.22-1.1 s. **Cap the boost to sniper+AR or bump `N` to 1536** — the 1024 ring buffer will evict live muzzle flashes in a late-circle fight. | M |
| 58 | Camera shake is vestigial: nothing you do shakes, and the hit-taken shake lasts ~61 ms | `runtime/3d/royale/fx.js:148/245/81` | Decay `dt*1.1`, scale hurt impulse by damage, add per-weapon shot punch and a kill punch. **Ship only together with DO-NOW #43 (slider + reduced-motion)** — per-shot punch on a 720 RPM SMG with no opt-out is an accessibility regression. | M |
| 59 | Recoil never accumulates — recovery outpaces every automatic's fire rate, so there is no spray pattern | `runtime/3d/royale/weapons.js:251/427` | Per-weapon `recoilRate`, skip recovery while `W.t - lastShotT < 0.12`, and **hard-cap the accumulator at 0.09 rad** — the cap is what prevents the documented "my pistol doesn't work" regression at `:422-425`. | M |
| 60 | Bots have no concept of supply drops; the crate bursts into floor loot the instant it lands | `bots.js:36` + `loot.js:271` | (a) `supplyDropLanded` subscription + scored `s.SUPPLY` between LOOT and ENGAGE, expiring at 90 s, **clearing on arrival within 4 m**; (b) make the landed crate a 2 s hold-E channel reusing `CHEST_OPEN_S`. Ship (a) and (b) separately. Merges both findings. | M |
| 61 | Bots cannot land a deliberate headshot (aim point sits 0.29 m under the head band) and never crouch | `bots.js:567` + `bots.js:347` | Tier-weighted `bb.aimHigh` re-rolled per burst → `aimY = tp.y + (aimHigh ? 1.62 : 1.15)`; set `inp.crouch` in camp/hold/snipe branches. **Do these together** — crouching drops the head to 1.55 m. Start top-tier head chance at 0.25, not 0.45 (tier-5 sniper headshot = 262 dmg). | M |
| 62 | Shotgun is the weakest weapon at its own optimal range (105 DPS vs SMG 204) **and** rare/epic shotguns deal identical damage | `runtime/sim/royale.js:56` | One data edit: `damage: 13, rpm: 80, reloadS: 3.0` → 117/blast, monotonic rarity ladder, re-ranks above the pistol. Merges both findings; selftest only asserts `spreadDeg`, which is untouched. | S |
| 63 | Shadows hard-terminate at 55 m with no cascade | `runtime/3d/ffg_royale3d.js:115` | `SHADOW_EXT = {low:55, medium:110, high:160}` — widening changes texel density, not batch count. Re-check `bias -0.0004` / `normalBias 0.02` for acne at 2-3× texel size, after #22's `sc.far` change. | S |
| 64 | Bot-snapshot LOD is measured from the HOST's player — a guest fighting a distant bot receives it at 2 Hz | `runtime/3d/royale/net.js:320` | Build an `eyes[]` list of human positions per tick, `far = eyes.every(...)`. ≤7 extra distance tests per bot. Interacts with #54's event budget. | S |
| 65 | Match clock free-runs per client and is synced exactly once — the storm circle is elsewhere on every machine | `runtime/3d/royale/net.js:276` | Host broadcasts `clock` every ~2 s; guest **slews forward-only** (`W.t` is the timestamp base for `lastHurtByActorT`, `lastDamageT`, cooldowns — a backwards jump suppresses damage credit). Verify storm `ctl.warned` doesn't double-fire. | M |
| 66 | Exterior roof ramps run 45-63° (run derived from building WIDTH, not climb) | `runtime/3d/royale/maps.js:547/559` | `run = max(fw-1.6, climb/tan(38°))`. Watch for clipping in Downtown (towers 55-125 m apart); switchback is the fallback. | M |
| 67 | No structure on any map has an opening at eye level — the tower window band starts 0.88 m above the player's eye | `runtime/3d/royale/maps.js:519` | Split upper-floor walls into sill (1.00 m) + header, leaving fy+1.00..fy+2.30 open on two of four walls. **Also opens bot LOS (`losBlocked` tests the same colliders) — playtest, don't just ship.** | M |
| 68 | Lagoon Docks' only building sits below sea level; Lakeside Cabins' pier is buried 2.3-8.4 m in terrain | `maps.js:790` (hut) + `maps.js:563` (pier) | Add water/terrain guards **and** relocate the call sites in the same change — the guard alone silently deletes the POI. Ship the absolute lake carve (`maps.js:152`) separately; it moves every heightAt0-dependent placement on Deepwood. | M |
| 69 | `antialias:true` allocates a 4× MSAA backbuffer nothing renders into (everything goes through the composer) | `runtime/3d/ffg_kernel_3d.js:39` | `{ antialias:false, preserveDrawingBuffer:true }` — pixel-identical, frees the buffer. For real AA, port `SMAAPass` from `pipeline/engine/runtime/3d/ffg_kernel_3d.js:24/203`. **Land in pipeline/engine first, then propagate.** | S |
| 70 | Procedural albedo CanvasTextures are decoded as linear | `runtime/3d/royale/maps.js:78` | `map.colorSpace = THREE.SRGBColorSpace` on `_fieldTex` only (not the normal map at :89). Re-check `terrMat.normalScale` (:303) and struct normalScale (:1067) in the same pass — the world gets visibly darker. | S |
| 71 | Default drop is 26 s, 19 s of it a canopy hanging over an LZ you were teleported onto | `runtime/3d/royale/player.js:665` + setDropTarget | Lever (a): lower the auto-deploy floor 110 → 55. Lever (b) offset the glide start like `bots.js:98` — needs the `lim` clamp kept and a water check. Tuning; do (a) first. | S |
| 72 | Low-HP bots with no consumables always fight to the death (`s.FLEE` gated on `heals`) | `runtime/3d/royale/bots.js:164` | `s.FLEE = ... ? (heals ? 86 : 72) : 0;`. Ship the 72 alone, verify a cracked bot disengages and returns, then add loot-seeking flee destinations. | S |

---

## 3. NICE TO HAVE

| # | Title | file:line | Fix | Size |
|---|---|---|---|---|
| 73 | No impact decals anywhere — all trace of your shots is gone in 0.4 s | `fx.js:111` | Pooled 120-instance decal mesh; **first cut = ground-only flat decals for `surface==="dirt"`** (no weapons.js normal plumbing). Must re-adopt into `W.group("fx")` in update() like the guard at `:199`. | M |
| 74 | Ground loot has no rarity beam; the chest beam is deleted the instant it spills | `loot.js:122` | Parameterised `chestGlow()` + shared cylinder geo with per-rarity cached materials keyed like `ringMats`. Must explicitly `beam.visible=false` past 90 m — the existing cull only skips bob/spin. | M |
| 75 | Opening the map (M) hides storm countdown, HP and ammo behind a 90% overlay | `hud.js:1946` | Header strip refreshed inside the existing 400 ms interval; `fmtT`/`aliveCount` already in scope. | M |
| 76 | Arm-pose layer has no blending — every ADS, shot and reload snaps in one frame | `player.js:985-1005`, `pose.js:133-150` | `_armPrevMode`/`_armBlend` over ~0.18 s, lerp the direction triples before `aim()`, drop `bl` to ~0.35. | M |
| 77 | No `scene.environment` anywhere — every metalness>0 surface reflects black | `maps.js:345` | Real PMREM from the sky dome, added to `disposeMapResources`. Low payoff until #78 lands; #31 already recovers the visible half. | M |
| 78 | One material template serves ~30 structure colours — lamps and the lighthouse beacon are brick-normal-mapped boxes | `maps.js:1066` | Per-colour EMIT/METAL profiles in the batch loop. Sequence after #77. | M |
| 79 | Corpse is deleted in a single frame with no sink or fade, contradicting the code's own comment | `player.js:1136` | Hold the clamped death pose ~3 s, then `kernel.tween` position.y −1.2 over 1.2 s; force-remove on match teardown. | S |
| 80 | Reward track is 3 unlocks and empty forever after level 10 | `hud.js:517/2092` | `else` branch rendering a lifetime-milestone goal off `W.progress.career`; same string replaces the "RANK UP" fallback. Delete or implement the fake "SEASON 1" label. | M |
| 81 | Nothing tracks what day it is — no reason to come back tomorrow | `hud.js:1081/1088` | `lastPlayedDay` + `dayStreak` in `loadProgress`/`recordMatch`; regenerate challenges on day change; "RESETS IN Xh" on the XP strip. | M |
| 82 | Challenges are deterministic by level — the same three cards for 7-9 matches | `hud.js:1123` | Seeded shuffle of `CHAL_POOL` off `W.seed` via `K.mulberry32` (verify the export first). | S |
| 83 | Quick Match pays ~3× the XP/hour of Battle Royale | `hud.js:2082` | `const mk = W.mode === "quick" ? 0.35 : 1;` on the placement + victory terms only. Sanity-check typical payouts. | S |
| 84 | Career record is write-only for damage and hours | `hud.js:514/2092` | Append both to the two render strings. Add `shotsFired/shotsHit` to `CAREER0` now if lifetime accuracy is ever wanted — it cannot be back-filled. | S |
| 85 | First-run How To Play fires on an uncancelled 500 ms timer (can pop after a match starts) | `hud.js:602` | Store `R._introT`, clear in `showLoading` and the mode-card onclick; add `"howto"` to the teardown array. | S |
| 86 | How To Play hard-codes default keys while promising they are rebindable | `hud.js:2177/778` | `keyLabel(physFor(W,"KeyM"))` etc.; derive the sprint line from `W.settings.sprintToggle`. **Display only — the toggle default stays true.** | S |
| 87 | Compass repaints at 10 Hz because it is gated behind the minimap throttle | `hud.js:1473` | Hoist `drawCompass` above the `if (mmT > 0.1)` block. | S |
| 88 | Minimap, compass and big map ignore devicePixelRatio | `hud.js:1219` | One `dpr` const, backing store × dpr, CSS box pinned, `ctx.setTransform` at the top of both draw fns. | S |
| 89 | Returning to the menu restores the sun and tone mapper but never the hemisphere light | `hud.js:184-188` | Traverse for `isHemisphereLight` and set warm golden-hour values; either populate `_lightDefaults` properly or delete it and its false comment. | S |
| 90 | Three maps share one hard-coded sun/hemisphere — map identity is a sky hex and 24% fog | `maps.js:272` | `sun{}`/`hemi{}` per MAPS entry; `_sunOff` is re-captured after `buildMap` so `followShadow` inherits it. | S |
| 91 | Sky dome is a two-stop ramp with no sun disc; Savanna's zenith computes to grey | `maps.js:337` | Per-map `zenith` + a `sunDir` uniform with a Mie lobe and ~1° disc. Fragment-only on a 24×16 dome. | M |
| 92 | Sky islands — "Final Drop's signature look" — are the only untextured surfaces in the world | `maps.js:1009` | Reuse cached `groundTex`, scale the **geometry** uv attribute (not a texture clone — `disposeMapResources` would free the shared upload). Add `rock.receiveShadow`. | S |
| 93 | Isla Viva has zero multi-storey interiors | `maps.js:744` | Two `tower()` calls (Cliff Temples, Coco Village market hall). Hollowing the landmarks is a separate job. | S |
| 94 | Storm wall texel density swings ~84× over a match | `storm.js:80` | `ctl.tex.repeat.x = max(6, round(2π·radius/40))`; compensate `offset.x` scroll at `:71` or the early-game scroll rate silently changes. | S |
| 95 | Landing produces zero FX — 50 touchdowns a match are visually silent | `fx.js` wireEvents | `landed` → dust/water ring by `pos.y <= waterY+0.4`; `hardLand` → bigger burst + camera dip. Self-contained. | M |
| 96 | Anim LOD's `timeScale=0` freeze saves only 39%, and pose.js allocates scratch arrays per call | `player.js:951`, `pose.js:141` | `mixer.userData.paused` + a kernel skip, module-scope scratch arrays. **Do NOT ship the audit's 250 m visibility cull — scoped snipers engage past it.** | S |

---

## 4. SKIP

| Title | file:line | Why |
|---|---|---|
| Clip transitions zero the stride phase (walk→run foot pop) | `ffg_kernel_3d.js:278` | Medium risk against a shared entry point used by all 14 3D games, for a clip-dependent payoff that has to be eyeballed. This file is a per-game copy that the pipeline may regenerate away — the fix belongs upstream and is not worth the round here. |
| No creatures or wildlife anywhere | `maps.js:373` | A new `ambient.js` module with per-map flocks and teardown wiring for zero gameplay effect. The audit itself declines to spend the round on it. |
| Crouch idle freezes the crouch-WALK clip on an arbitrary frame | `player.js:964` | Requires manually measuring a planted-feet timestamp off each skin's `_crouch.glb` (6 skins) plus a change to `playAnim`'s same-key early-return path. Payoff is one camping player's foot. |
| Locker preview disc metalness / dead `envMapIntensity` lines | `hud.js:670` | Not worth a standalone task — it is the menu preview only, and a real PMREM should land once at kernel level (#77). If touched at all, fold the `metalness 0.65 → 0.1` line into #77's commit. |
| Real bullet-drop ballistics for sniper/AR | `weapons.js:416` | `bots.js:581` compensates for `arc` weapons only, so adding drop makes all 49 bots shoot low at range. Take the documentation fix (#46) instead. |
| Absolute lake carve on Deepwood | `maps.js:152` | Shifts every `heightAt0`-dependent placement on the map (town minH guards, scatterTrees, loot) and forces a full POI re-walk. The pier terrain guard (#68) fixes the visible defect without it. |
| Per-victim accumulating damage numbers (Apex-style) | `fx.js:174` | Needs a live-number registry with invalidation on death and on `reset()`. The jitter + variable rise (#29) captures nearly all the readability win for two lines. |
| Career stats panel | `hud.js:1095` | Only `shotsFired/shotsHit` would make a panel interesting and they were never recorded, so it cannot be back-filled. Ship the one-line display (#84) and revisit only if the counters get added. |

---

**Sequencing notes for the queue:**
- #43 (shake slider) must land **before or with** #58 (shake impulses).
- #31 must land **before** #77/#78 (env-map chain), and #22 before #63 (shadow chain).
- #49 must touch `sim/royale.js` **and** `bots.js` in one commit.
- #61's two halves ship together; #55 and #27 are the same perf area — do #27 now, #55 as the real fix.
- Every constraint holds throughout: no building/harvesting, `randomMap()` stays (#40), `sprintToggle` stays true (#21, #86), nothing here publishes the game.