# Working shot output (bootcheck/battery iterations/critic inbox) — local
# artifacts of the verify loop, regenerated by _harness; NOT design refs.
# Repo lesson (2026-05-11 filter-repo purge): bulk PNG/GLB working sets stay
# out of history. scores.jsonl (critic ledger) IS tracked — negation below.
_shots/
!_shots/scores.jsonl
__pycache__/
*.pyc
